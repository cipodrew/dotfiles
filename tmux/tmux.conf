#1000set -g default-terminal "xterm-256color"
#set-option -ga terminal-overrides ",xterm-256color:Tc"
#set-option -g default-terminal "screen-256color" #nvim suggestion
# set -g default-terminal "tmux-256color"
# set -g terminal-overrides "xterm-256color"
set -g default-terminal "screen-256color"

#set-option -g detach-on-destroy off   #useful when using session managers

set -g prefix ^Space
# set -g prefix ^A
set -g base-index 1          # start indexing windows at 1 instead of 0
set -g pane-base-index 1     # start indexing panes at 1 instead of 0
set-window-option -g pane-base-index 1
set -g renumber-windows on   # renumber all windows when any window is closed

set -g history-limit 1000000 # increase history size (from 2,000)
set -g mouse on              # enable mouse support
set -g set-clipboard on      # use system clipboard

#NVIM checkhealth suggestions
set-option -sg escape-time 10
set-option -g focus-events on

set -g default-terminal "${TERM}"
#setw -g mode-keys vi
set -g display-time 4000 # Increase tmux messages display duration from 750ms to 4s

#style and status bar
set -g status-position top       # macOS / darwin style
#set -g window-status-format '#[fg=gray,bg=#1e1e2e] #I #W'
#set -g window-status-last-style 'fg=white,bg=black'

# tmux display-popup # comando utile negli script

#set -g message-command-style bg=default,fg=yellow
#set -g message-style bg=default,fg=yellow
#set -g mode-style bg=default,fg=yellow


# MOVEMENT
# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

#same but vim keys
bind -n M-h select-pane -L
bind -n M-l select-pane -R
bind -n M-k select-pane -U
bind -n M-j select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

#same but vim keys
bind -n S-h  previous-window
bind -n S-l next-window

# Prefix + vim keys to switch window
bind h previous-window
bind l next-window
bind j previous-window
bind k next-window

# Reorder windows
bind-key -n C-S-Left swap-window -d -t -1
bind-key -n C-S-Right swap-window -d -t +1

# PANES
bind + split-window -v -c "#{pane_current_path}"
bind ù split-window -h -c "#{pane_current_path}"
bind ] split-window -v -c "#{pane_current_path}"
bind Enter split-window -h -c "#{pane_current_path}"
#source the config file with prefix shift+R
 bind R source-file "~/.config/tmux/tmux.conf" \;  display "Config reloaded 🚀"
#bind R source-file "$HOME/.config/tmux/tmux.conf"

#source a file in this config
# source-file /.config/tmux/style.conf

#### -------PLUGIN OPTIONS---- #####
#set -g @thumbs-key C

#### -------PLUGINS---- #####
set -g @plugin 'tmux-plugins/tpm' # load tpm # <pre>+shift+i install, <pre>+u update, <pre>+<alt>+u uninstall
#set -g @plugin 'fcsonline/tmux-thumbs' #<ctrl|shift>+c #default is <pre>+Space #easly jump to lines in terminal
set -g @plugin 'Morantron/tmux-fingers' # prefix+f goes into finger mode. J to jump mode
#https://github.com/joshmedeski/sesh # SESSION MANAGER check github for installation instructions
# or use tmux sessionx as an alternative https://github.com/omerxx/tmux-sessionx
#set -g @plugin 'tmux-plugins/tmux-yank'

#set -g @plugin 'christoomey/vim-tmux-navigator' # <ctrl>+hjkl # I actually like having separate commands for vim and tmux though

# Plugin to install catppuccino theme
# set -g @plugin 'catppuccin/tmux'
### PIN version until I migrate the config
set -g @plugin 'catppuccin/tmux#v0.3.0'
set -g @catppuccin_flavor 'macchiato'
# set -g @catppuccin_flavour 'macchiato'

#set -g @catppuccin_window_status_icon_enable "yes"
#set -g @catppuccin_icon_window_zoom ""
#set -g @catppuccin_window_current_text "#W"
#set -g @catppuccin_icon_window_last "null"
#set -g @catppuccin_icon_window_current "null"
#set -g @catppuccin_icon_window_mark "null"
#set -g @catppuccin_icon_window_silent "null"
#set -g @catppuccin_icon_window_activity "null"
#set -g @catppuccin_icon_window_bell "null"

set -g @catppuccin_window_status_icon_enable "no"
set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"



set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_default_fill "number"
#set -g @catppuccin_window_default_fill "none"
#set -g @catppuccin_window_middle_separator " "
#set -g @catppuccin_window_current_color "#ffd9f5" 
#set -g @catppuccin_window_default_color "#b4bcdb" 
#set -g @catppuccin_window_current_color "#ddb6f2" 

set -g @catppuccin_window_current_color "#bc8df7" 
set -g @catppuccin_window_default_color "#CAD3F5" 

#set -g @catppuccin_window_default_color "#5a649c" 
#set -g @catppuccin_window_current_color "#c6a0f6" 
#set -g @catppuccin_window_default_background "#333333"
set -g @catppuccin_window_current_background "#121111"

set -g @catppuccin_status_modules_right "directory session"
#set -g @catppuccin_status_modules_right "application directory session"
#set -g @catppuccin_maximized_text "null"

set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_status_left_separator "█"
set -g @catppuccin_status_right_separator "█"
set -g @catppuccin_directory_color "#8aadf4"
set -g @catppuccin_session_icon "session"

# Per qualche motivo non funzionano
set -g @catppuccin_pane-active-border-style "fg=#{thm_magenta}"
set -g @catppuccin_pane-border-style 'fg=brightblack,bg=default'

set -g @fingers-key f
# set -g @fingers-show-copied-notification 1
#----------------
#set-environment -g TMUX_PLUGIN_MANAGER_PATH '/some/other/path/' 
#cambia il path in cui sono instllati i plugin By default, TPM installs plugins in a subfolder named plugins/ inside $XDG_CONFIG_HOME/tmux/ if a tmux.conf file was found at that location, or inside ~/.tmux/ otherwise.

# AUTOMATIC TPM INSTALL if not installed already in ~/.tmux/plugins/tpm 
#if "test ! -d ~/.tmux/plugins/tpm" \
#   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# run "$HOME/.config/tmux/plugins/tpm/tpm" # default ma io non lo voglio in .config
run '~/.tmux/plugins/tpm/tpm' # always at end of file 

